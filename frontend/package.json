{
  "name": "t9",
  "version": "1.1.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "vite build",
    "clean": "rm -rf node_modules dist"
  },
  "devDependencies": {
    "vite": "^2.6.7"
  },
  "dependencies": {
  }
}
